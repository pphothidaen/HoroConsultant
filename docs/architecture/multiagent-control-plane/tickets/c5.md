# C5 — Shadow Migration, Governance, and Cutover

`MAREF-050..055` are blocked behind C3/C4. `MAREF-056` is additionally excluded
from the session parent grant and needs fresh owner approval in the executing
root session. MAREF-057 rollback/restoration drill needs a separate fresh P4
grant. No C0 decision is a deployment authorization, and no concrete production
target or argv is frozen in C0.

| Ticket / objective | Severity / Work Effort | Owner and exact ownership | Status / dependencies | Acceptance criteria | Required evidence | Stop condition |
|---|---|---|---|---|---|---|
| `MAREF-050-SHADOW-MODE` — compare without authority change | CRITICAL / XL | `developer`; new `project/orchestration/config.py`, new `project/orchestration/migration/shadow.py`, then exactly one composition hunk in `project/orchestration/bootstrap.py` after MAREF-036 freezes it | `BLOCKED — C3/C4 + 033/036 FREEZE`; MAREF-033,036,037,044 | Typed config, not an environment flag, enables effect-free shadow; legacy alone remains authority; ControlPlane derives commands/projections and typed divergence only; disabling restores exact path | deterministic shadow/no-effect report, config/one-hunk diff, dispatcher/bootstrap freeze digests and platform sample matrix | Stop on project/core config/main edits, env authority flag, overlapping dispatcher/bootstrap editor, dual write/authority, shadow effect or unsafe disable |
| `MAREF-051-LEGACY-IMPORT` — import and pre-frozen cutover tooling | HIGH / L | sequential sublanes: `developer` owns new `scripts/import_control_plane_legacy.py`, `scripts/run_control_plane_post_cutover_verification.py`, `scripts/rollback_control_plane_cutover.py`, `project/schemas/control-plane/import-manifest-v1.schema.json`, `project/schemas/control-plane/cutover-manifest-v1.schema.json`; then `business_analyst` owns new `docs/templates/CONTROL_PLANE_IMPORT_MANIFEST_V1.json`, `docs/templates/CONTROL_PLANE_CUTOVER_MANIFEST_V1.json`; each import run alone writes `project/tests/artifacts/control_plane/import/<import_id>/import-manifest-v1.json` | `BLOCKED — 050`; MAREF-050 | Import binds source/digest/schema/importer/cutoff and authority epoch; repeat is idempotent; changed bytes need new manifest; cutover schema/tooling defines closed late-bound target/revision/file/digest/action/grant/argv fields but its C1-C5 template contains no concrete production value; history is not reinterpreted | offline import/re-import/changed-source/tool-schema tests, source/artifact digests, manifest inventory and proof the cutover template contains no executable argv/target | Stop on mutable source, fabricated old event, hard-coded production command/target, secret/PII retention, destructive cleanup or unowned artifact path |
| `MAREF-052-REPLAY-RECONCILE-QA` — zero-divergence gate | CRITICAL / XL | `qa_tester`; new `tests/control_plane/test_shadow_replay.py`, `tests/control_plane/test_control_plane_migration_tools.py` and `tests/control_plane/fixtures/migration/**` only | `BLOCKED — 025,050,051`; MAREF-025,050,051 | At least 100 deterministic fixtures replay with zero unexplained divergence; shadow has no effects; import/cutover tooling, authority epoch, crash/restart and exact-manifest rejection are covered | fixture count/digests, replay/shadow/tooling report and failure reproducer inventory | Stop on fewer than 100 fixtures, unexplained divergence, non-determinism, non-exact manifest, missing provenance, dual authority or history rewrite |
| `MAREF-053-LOAD-CAPACITY-QA` — multi-host lease/capacity tuning | CRITICAL / L | `qa_tester` is sole source editor for new `tests/control_plane/test_load_capacity.py` and exact profiles `tests/control_plane/load/profiles/single_host_baseline.yaml`, `tests/control_plane/load/profiles/multi_host_contention.yaml`, `tests/control_plane/load/profiles/authority_partition_recovery.yaml`, `tests/control_plane/load/profiles/capacity_saturation.yaml`, `tests/control_plane/load/profiles/lease_timing_telemetry.yaml`; `devops` is run-only and writes `project/tests/artifacts/control_plane/load/<run_id>/load-report-<sha256>.json` | `BLOCKED — 052`; MAREF-024,037,052 | Caps never exceed; 429/capacity recovery, authority partition and 120/40/no-grace telemetry are measured/versioned; no stale fence or double effect; report content digest matches filename | offline/staging metrics, exact profile digests, race/fault results, content-addressed report and proposed policy | Stop on paid/production load, DevOps source edit, unnamed profile/report, over-capacity, stale success or insufficient telemetry |
| `MAREF-054-GOVERNANCE-SYNC` — policy/skill/catalog and repo-only sync | HIGH / L | sequential subphases only: (A) `developer` owns `scripts/sync_sdlc_agents.py` and `project/tests/test_ai_agent_ecosystem_sync.py` to add fail-closed repo-only/skill-scoped sync plus exact output manifest; (B) `business_analyst` owns new `.agents/rules/20-multiagent-control-plane.md`, `.claude/rules/multiagent-control-plane.md`, `.agents/skills/multiagent-control-plane/SKILL.md`, `.agents/skills/multiagent-control-plane/evals/evals.json`, existing `.agents/AGENTS.md`, `CLAUDE.md`, `docs/templates/MULTIAGENT_PROMPT_COMMAND.md`; (C) scoped sync alone owns generated `.antigravity/skills/multiagent-control-plane/SKILL.md` and `project/tests/artifacts/control_plane/governance/maref-054-repo-sync-manifest.json` | `BLOCKED — SOURCE/QA FREEZE + REPO-ONLY SYNC PREREQUISITE`; MAREF-052,053 | Rule/skill stay narrow; scoped sync manifest predicts and then records only the named repo mirror; umbrella/global `--sync` is not run; read-only ecosystem checks pass after freeze; no `.codex` or external home mutation | tooling tests, pre/post output manifest, scoped diff, read-only check, catalog/eval audit and secret scan | Stop if repo-only mode is absent, manifest names another path, umbrella sync would rewrite agents/global `~/.gemini`/`~/.agy*`, source not frozen, manual generated edit or external sync is attempted without separate approval |
| `MAREF-055-SAFETY-REVIEW` — pre-cutover read-only verdict | CRITICAL / M | `code_reviewer`; read-only exact changed-file/artifact manifest for MAREF-010..054 only | `BLOCKED — 054`; MAREF-044,052..054 | Reject missing direct dependency pins, CAP/stale-read semantics, stale-success, fewer than 100 fixtures, non-exact manifests, secrets, split/dual authority, unbound approval, replay, stale fence, force bypass, compatibility gap or unresolved critical/high finding | read-only report, exact source/fixture/artifact digests and QA/scoped-sync/secret evidence references | Stop `BLOCKED` on any named gap/missing evidence; `NEEDS_HITL` on unresolved high-impact judgment; never repair in review lane |
| `MAREF-056-PRODUCTION-CUTOVER` — one-shot authority switch | CRITICAL / L | no current execution ownership; after fresh P4 HITL, `devops` may write only `project/tests/artifacts/control_plane/cutover/<cutover_id>-<manifest_sha256>/cutover-manifest-v1.json`, `database-backup-receipt-v1.json`, `deployment-receipt-v1.json`, `cutover-receipt-v1.json`; no live/repository source edit | `BLOCKED — FRESH HITL / EXTERNAL / LATE BINDING`; MAREF-055 plus new root-session target/action approval | Before any command, immutable manifest binds exact authenticated target, source revision, files/digests, migration/backup, expected/prior/new authority epoch and concrete deploy/verify/emergency-rollback argv; switch is one-shot with no dual authority; 051 tooling executes only the approved manifest; compatibility clock does not start | fresh P4 cutover grant, manifest digest/path, reviewer verdict, versioned backup/deployment/cutover receipts, health/version and exact rollback point | Zero mutation/command while target/revision/files/argv/digests/grant are absent; `NEEDS_HITL` on unknown target/credentials; an emergency rollback still requires its exact action to be present in the fresh grant, otherwise stop |
| `MAREF-057-POST-CUTOVER` — verify, drill rollback, accept authority | CRITICAL / L | in `project/tests/artifacts/control_plane/cutover/<cutover_id>-<manifest_sha256>/` only: `qa_tester` writes `post-cutover-verification-v1.json`; after a separate fresh P4 rollback-drill/restoration grant, `devops` writes `rollback-receipt-v1.json`; `orchestrator` is read-only verdict owner | `BLOCKED — 056 + FRESH P4 ROLLBACK-DRILL GRANT`; MAREF-056 | Pre-frozen 051 verification/rollback tools prove replay, outbox, lease/fence, session expiry, matrix, health/version and explicit rollback/restoration drill without deleting history; the 056 cutover grant cannot authorize the drill/re-cutover; final authority acceptance is recorded only after the read-only verdict, and only then starts v2 >=2 releases+90-day clock | versioned content-addressed verification/rollback receipts, separate fresh P4 grant, durable cursor/replay digest, platform results, rollback/restoration drill, final authority acceptance timestamp and compatibility calendar anchor | Zero rollback/re-cutover command if the separate grant or exact late-bound argv is absent; roll back on critical mismatch only under exact approved emergency action; `NEEDS_HITL` for credentials/external judgment; never delete legacy/history |

## C5 completion

The refactor is complete only after MAREF-057 evidence. C0-C4 local evidence
cannot substitute for CI, deployment, live endpoint, database backup or
post-cutover proof.
