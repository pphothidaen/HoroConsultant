# C4 — HITL Approval and Effect Saga

All tickets are `BLOCKED`. Ticket39 remains the owner of current admin/HITL
routing until its mandatory metaphysical scope audit, owner sign-off, source
freeze and QA complete. `NEEDS_HITL` freezes E2-E4.

| Ticket / objective | Severity / Work Effort | Owner and exact ownership | Status / dependencies | Acceptance criteria | Required evidence | Stop condition |
|---|---|---|---|---|---|---|
| `MAREF-040-APPROVAL-SERVICE` — issue/consume/revoke grants | CRITICAL / L | `developer` (security lane); new `project/orchestration/application/approval_service.py` only | `BLOCKED — C2 + AUTH`; MAREF-012,021,023,025 and Ticket39 authenticated identity contract | Server derives issuer/reviewer/session; atomic max-use consumption, expiry/revocation/supersession and cross-session rejection; P0-P4 policy enforced without self-approval or AP fallback | unit/integration results for replay, over-use, scope mismatch, spoofing, restart/authority expiry and concurrent consume | Stop on payload reviewer authority, wildcard grant, session ambiguity, self-approval, no-authority grant or split transaction |
| `MAREF-041-EFFECT-SAGA` — E0-E4 orchestration/compensation | CRITICAL / XL | `developer`; new `project/orchestration/application/effects/saga.py` and `models.py` only | `BLOCKED — 013,040`; MAREF-013,023,024,040 | Implements exact E0→P0 through E4→P4 SagaCommand/Receipt contracts; each stage is idempotent and separately approved/leased/fenced as required; unknown reconciles; failing execution compensates before terminal and post-terminal remediation is a new linked execution; NEEDS_HITL freezes E2-E4 | reducer/integration/fault tests for every stage/outcome, duplicate, partial failure, terminal immutability, linked compensation and unknown reconciliation | Stop on hidden chain, force, retrying unknown, delete/history-as-rollback or effect without required grant+lease+fence |
| `MAREF-042-HITL-INTEGRATION` — route current HITL through Saga | CRITICAL / L | `developer`; existing `project/hitl_router.py` only | `BLOCKED — TICKET39 + 041`; MAREF-040,041 plus `TICKET-RELEASE-COMPLETE-20260826-39-ADMIN-HITL-ROUTING`, scope audit/sign-off and `...-39-QA` source freeze | Current direct JSONL/FAISS/background-training path becomes command/Saga proposals; `force` cannot bypass P-class approval; `required_human_review` and scope audit gate preserved | focused HITL/auth/Saga regression, pre/post behavior map and exact source-freeze digest | Stop on missing scope-audit pass, overlapping Ticket39 editor, force/auto training, unauthenticated reviewer or direct E2-E4 mutation |
| `MAREF-043-COMPENSATION-ADAPTERS` — versioned file/index/training effects | CRITICAL / XL | `developer`; existing `project/rag/external_finetune.py`, `project/rag/vector_store.py`, `project/mlops/training/finetune_orchestrator.py` and new `project/orchestration/adapters/effects/**` only | `BLOCKED — 041,042 SOURCE FREEZE`; MAREF-041,042 | JSONL/index use immutable generations and atomic activation pointer; MLX/Kaggle/OpenAI/Supabase effects bind SagaCommand/Receipt idempotency; paid/external calls require exact approval; unknown training reconciles; compensation never deletes audit/external history | offline/mocked adapter tests, generation/activation compensation proof, duplicate/unknown reconciliation and zero-cost cases | Stop on network/paid action during implementation, destructive overwrite, unbound job, raw secret/evidence or non-compensatable effect |
| `MAREF-044-GOVERNANCE-QA` — security/adversarial Saga gate | CRITICAL / XL | `qa_tester` plus read-only `code_reviewer`; new `tests/control_plane/test_approval_security.py`, `test_effect_saga.py`, `test_hitl_integration.py` only | `BLOCKED — 040..043`; MAREF-040..043 | Cross-session/spoof/self-approval, stale fence, duplicate training, partial Saga, force/auto bypass, P0-P4, E2-E4 freeze and compensation are rejected/proven; Ticket39 domain cases remain green | focused/adversarial report, reviewer verdict, failure injection matrix and sanitized evidence | Stop `BLOCKED` on any bypass/data divergence; `NEEDS_HITL` on unresolved security/domain conflict |

## C4 checkpoint exit

Automatic, threshold, background and forced training remain blocked until this
checkpoint passes. Local dry-run structure is not approval or external outcome.
