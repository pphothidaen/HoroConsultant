# C1 — Contract-first Foundation

MAREF-010 is `READY — NW-SESSION-001 CHILD GRANT REQUIRED`. The validated
runtime limitation remains: mutation objective/scope binding and a governed
receipt cannot be proven for the selected alias route. Decision digest
`cb2cf84444b699a642969e5fb4be43829d39548b87b66531ab8f87fff5b01d6d`
validated; snapshot digest
`5611f252f987aef0e6f5c54c0d60e19d0aacce2cc110e5ba3d2989a4934fc39b`
is candidate/non-live. Only read-only/high config is approved; mutation mode
does not enforce it, objective text is arbitrary/unbound, and self-declared
temporary approval is prohibited. No alias executed and no lifecycle file was
created. MAREF-011+ remains `BLOCKED`.

The user-approved native-fallback parent waiver `NW-SESSION-001` covers
`MAREF-010..055` only through separately derived one-ticket children. Planned
child `NW-SESSION-001/MAREF-010/1` is not yet issued: it binds only the lifecycle
path below, the lifecycle-contract action, native `business_analyst` route
intent `gpt-5.6-sol/xhigh`, a frozen-input scope digest, `max_uses=1`, and this
root-session expiry. It accepts no alias/provider ExecutionReceipt for that
native child, but requires a native WorkResult, scoped diff/evidence and an
independent reviewer `PASS`. No execution is authorized until the child grant
is issued. Approval was recorded at `2026-08-26T12:11:01+07:00` and binds
`current runtime-enforced collaboration root thread /root`; no opaque provider
or session ID is claimed. See
[ADR-004](../adr-004-session-scoped-approval.md).

After the implementation WorkResult is `DONE` and independent review is
`PASS`, MAREF-010 requires a separate exact-file/hunk, single-use delegated
local-commit child before final completion. No `BLOCKED`/`NEEDS_HITL` or
unrelated content may be committed; root does not implement or commit, and no
push is automatic. This waiver-record support mutation is not a numbered MAREF
completion and must not be committed.

| Ticket / objective | Severity / Work Effort | Owner and exact ownership | Status / dependencies | Acceptance criteria | Required evidence | Stop condition |
|---|---|---|---|---|---|---|
| `MAREF-010-LIFECYCLE-CONTRACT` — formal FSM/commands | CRITICAL / M | `business_analyst`; new `docs/architecture/multiagent-control-plane/contracts/lifecycle-v1.md` only | `READY — NW-SESSION-001 CHILD GRANT REQUIRED`; MAREF-000..005 plus both C0 PASS WorkResults | Execution/Approval/Lease transition tables, command preconditions, immutable terminal/linked-compensation semantics and invariants are machine-mappable; stale-read metadata and required 503/409/403/202/429 semantics are explicit | issued child grant, native WorkResult, scoped diff/evidence, reviewed contract, transition/rejection coverage matrix, ADR-CAP trace and independent reviewer `PASS`; no alias/provider ExecutionReceipt is claimed | Stop before mutation until `NW-SESSION-001/MAREF-010/1` is issued with exact action/path/role/scope digest/max-use/expiry; `NEEDS_HITL` on new high-impact state semantics |
| `MAREF-011-EVENT-ENVELOPE` — closed control-plane event schema | CRITICAL / M | `developer`; new `project/schemas/control-plane/event-envelope-v1.schema.json` only | `BLOCKED — 010`; MAREF-010 | Closed platform-neutral schema binds tenant/run/task/attempt/event, catalog/version, sequence, correlation/causation, payload digest, previous/event hash, authority epoch and timestamp; adapter/provider values are namespaced opaque metadata outside reducer inputs | metaschema pass, valid vectors, unknown-field/hash/catalog negatives plus root adapter field, metadata promotion and canonical-only-in-metadata rejection | Stop before edits without derived grant/Rule18; fail on frozen TDD overlap or any adapter field influencing canonical reduction |
| `MAREF-012-APPROVAL-GRANT` — parent/child/revocation schema | CRITICAL / M | `developer`; new `project/schemas/control-plane/approval-grant-v1.schema.json` only | `BLOCKED — 010`; MAREF-010 | Closed schema binds canonical session, ticket/action/owner/files/scope digest, issuer, expiry/max-use/use, attempt and revocation/supersession | valid vectors plus cross-session, over-use, spoofed issuer and digest-mismatch negatives | Stop on any wildcard/transferable/self-issued grant or missing server identity |
| `MAREF-013-EFFECT-SAGA-CONTRACTS` — lease/fence and Saga command/receipt | CRITICAL / L | `developer`; new `project/schemas/control-plane/effect-lease-v1.schema.json`, `project/schemas/control-plane/saga-command-v1.schema.json`, `project/schemas/control-plane/saga-receipt-v1.schema.json` only | `BLOCKED — 010`; MAREF-010 | Lease binds tenant/run/task/effect/stage, DB attempt/fence/clock, holder/capacity/policy/supersession; SagaCommand binds class/stage, command/version/idempotency, approval, lease/fence, input/causation digests; SagaReceipt records durable acceptance/event IDs and typed accepted/succeeded/failed/unknown/compensating/compensated/compensation-failed/rejected outcome with reconciliation/linked-compensation fields | metaschema/vectors for every stage/outcome plus stale fence, late renew, client token, blind retry, unknown and compensation negatives | Stop on client/provider authority, unbound effect/approval/fence, retry of unknown, deletion-as-compensation or queue-only accepted receipt |
| `MAREF-014-COMPATIBILITY-CONTRACT` — capability/loss declarations | HIGH / M | `developer`; new `project/schemas/control-plane/platform-capability-v1.schema.json` and `adapter-loss-v1.schema.json` only | `BLOCKED — 010`; MAREF-011..013 | Closed capability declares adapter/version, contracts/transports, stream/resume/cancel/idempotency/read-only/portable receipt, ordering/durability scopes, concurrency/payload limits, billing class, retention mode, observed/expiry times, provenance digest, preserved/unavailable/dropped fields and typed expired/unknown/unsupported/unavailable; opaque metadata cannot supply authority | valid declarations for all matrix rows and negatives for expired/unknown-as-success, root adapter field, metadata promotion, undeclared loss and invented proof | Stop if a platform silently drops authority fields, infers proof, treats stale capability as success or exposes adapter data to reducers |
| `MAREF-015-CONTRACT-QA` — closed-schema negative gate | CRITICAL / XL | `qa_tester`; new `tests/control_plane/test_contract_schemas.py` and `tests/control_plane/fixtures/contracts/**` only | `BLOCKED — 011..014`; MAREF-011..014 | 100% valid vectors pass; unknown/duplicate/non-finite, cross-catalog/session/tenant, stale fence/epoch, replay/over-use/digest, Saga unknown/blind retry, provider-root/canonical-in-metadata/metadata-promotion, expired capability and stale-success vectors fail deterministically | focused pytest report, fixture inventory digest, transition/schema coverage and scoped diff | Stop `BLOCKED` on any false accept, stale-success, opaque-authority promotion, flaky result or ownership overlap |

## C1 checkpoint exit

All six ticket groups and every owned contract/QA artifact must freeze with one
editor per path.
Only then may the orchestrator mark C2 candidates `TODO`; no source core work may
start from prose or schemas without C1 evidence.
