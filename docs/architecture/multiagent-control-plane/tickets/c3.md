# C3 — Platform Adapters and Legacy Integration

All tickets are `BLOCKED — C2` and retain the current Result Contract v2 and
QOBS ownership boundaries. Provider identities remain correlation only.

| Ticket / objective | Severity / Work Effort | Owner and exact ownership | Status / dependencies | Acceptance criteria | Required evidence | Stop condition |
|---|---|---|---|---|---|---|
| `MAREF-030-NATIVE-ADAPTER` — native collaboration envelope | HIGH / M | `developer`; new `project/orchestration/adapters/platform/native.py` only | `BLOCKED — C2`; MAREF-023,025 | Maps native task/result/mailbox metadata to contracts, declares unavailable provider proof and keeps native values namespaced/opaque | unit fixtures for full/partial/unsupported capability, secret redaction and metadata-promotion rejection | Stop on inferred authority, identity substitution, reducer-visible provider field or direct persistence write |
| `MAREF-031-CODEX-ADAPTER` — Codex v2 compatibility | CRITICAL / M | `developer`; new `project/orchestration/adapters/platform/codex.py` only | `BLOCKED — C2`; MAREF-014,023,025 | Preserves fail-closed JSONL/output-schema Result Contract v2, binds canonical run/task/attempt/fence and keeps thread ID opaque correlation only | current-v2 compatibility fixtures plus malformed/ambiguous/secret/stale-fence/metadata-promotion negatives | Stop if legacy parser is weakened, prose fallback appears, old receipt is reinterpreted or thread data drives transition |
| `MAREF-032-AGY-ADAPTER` — AGY native stream compatibility | CRITICAL / M | `developer`; new `project/orchestration/adapters/platform/agy.py` only | `BLOCKED — C2`; MAREF-014,023,025 | Preserves native stream-JSON dialect, one terminal result and validated-in-process-only boundary; binds canonical IDs/fence while conversation ID remains opaque | current-v2 AGY fixtures, event/cardinality/secret/fence/metadata-promotion negatives | Stop on free-form inference, portable/offline success claim or conversation-ID authority |
| `MAREF-033-DISPATCHER-BRIDGE` — lease/fence-gated spawn | CRITICAL / L | `developer`; existing `scripts/multiagent_prompt_command.py` only | `BLOCKED — C2 + QOBS SOURCE FREEZE`; MAREF-024,030..032 and `TICKET-ALIAS-RC2-004-QOBS-DISPATCH` + `...-QOBS-QA` | Spawn requires canonical current lease/fence, grant, Rule18/QOBS and idempotent command; existing parser/privacy/receipt-v2 gates remain; result proposal cannot transition directly | focused dispatcher/current-v2/QOBS/control-plane integration report and source-freeze digest | Stop on overlapping QOBS editor, copied quota/grant, root execution, stale fence or receipt regression |
| `MAREF-034-SCHEDULER-BRIDGE` — derived canonical eligibility | CRITICAL / L | `developer`; existing `scripts/multiagent_ticket_scheduler.py` only | `BLOCKED — 024,033 + QOBS SOURCE FREEZE`; MAREF-024,033 and `TICKET-ALIAS-RC2-004-QOBS-SCHEDULER` + `...-QOBS-QA` | Scheduler queries/derives canonical dependency, quota/HITL/grant/lease capacity; caller-copied `quota_passed`/`hitl_passed` cannot authorize; ordering remains Rule 11 after gates | scheduler/QOBS/race/ownership regression with invalid-copy negative cases | Stop on concurrent ownership, snapshot as authority, unknown quota execution or comparator-before-gate |
| `MAREF-035-OPENAI-RESPONSES` — HTTP plus optional WS model I/O | HIGH / L | `developer`; new `project/orchestration/adapters/platform/openai_responses.py`; only if WS is implemented, a later sequential manifest sublane owns exactly `pyproject.toml`, `requirements.txt`, `uv.lock` after MAREF-021 dependency freeze | `BLOCKED — C2 + 021 MANIFEST FREEZE`; MAREF-014,021,023,025 | Mandatory HTTP fallback; optional WS enforces same-stream FIFO/cross-stream demux, 16 in-flight, 32 stream IDs, 60-minute rotation, store=false/ZDR recovery and typed context loss; WS implementation declares/pins `websockets` directly in all manifests, while HTTP-only startup neither imports nor requires it | deterministic fake-server protocol/reconnect/fallback/limit/secret cases, dependency/lock consistency if WS exists, and measured comparison | Stop on browser key, Realtime/WebRTC, WS-only or indirect-only dependency, manifest overlap, guaranteed latency claim or stream-as-run/session |
| `MAREF-036-NOTIFICATION-API` — REST commands, SSE and composition root | CRITICAL / L | `developer`; new `project/orchestration/adapters/api/**`, new `project/orchestration/bootstrap.py`, then exactly one bounded router-registration hunk in existing `project/main.py` only | `BLOCKED — C2`; MAREF-021,023,025 | One composition root creates the sole handler; authenticated REST calls it only; SSE publishes committed outbox events and resumes by durable cursor; stale reads expose version/sequence/authority_epoch/read_at/lag; 503/409/403/202/429 baseline is exact and 202 follows durable acceptance; no mandatory internal WS/network hop | API auth/contract/partition/stale/reconnect/outbox tests, exact main hunk, 202 durability proof and route/privilege audit | Stop on any other main edit, public privileged endpoint, notification-before-commit, queue-only 202, hidden staleness, AP mutation, direct persistence write or browser secret |
| `MAREF-037-PLATFORM-CONFORMANCE` — matrix-wide adapter gate | CRITICAL / XL | `qa_tester`; new `tests/control_plane/test_platform_conformance.py` and `tests/control_plane/fixtures/platforms/**` only | `BLOCKED — 030..036`; MAREF-030..036 | Every active row passes the complete capability contract or typed unsupported; macOS/Linux/non-POSIX, Docker/Vercel, providers/effects/reconnect/stale reads covered; canonical root isolation and opaque metadata non-influence proven | matrix report, fixture inventory digest, stale/API status matrix and zero unexpected skip | Stop on silent loss, invented evidence, untyped skip, expired/unknown-as-success, root adapter field, canonical-only-in-metadata, metadata promotion, platform authority or flaky network dependence |

## C3 checkpoint exit

C3 requires the QOBS and Ticket39-independent source ownership freezes it names.
OpenAI WebSocket performance is optional; protocol correctness and HTTP fallback
are mandatory. No active platform may be silently dropped to make the gate pass.
