# 🛡️ Mandatory Operational Rulebook for AI Agents & Subagents
> **Project:** HoroConsultant (Metaphysics Engine & Fine-Tuning Pipeline)  
> **Enforcement:** Mandatory for all Agent System Directives & Context Ingestions.

---

## 📌 Core Operational Commandments (ข้อกำหนดบังคับรันงานสำหรับ Agent ทุกตัว)

### 1. 📦 Pip Options Standard (ห้ามใช้ Option ที่ไม่มีอยู่จริงเด็ดขาด)
- **กฎ**: ห้ามใช้ `--no-progress-bar` บน `pip` ทุกเวอร์ชัน (ทำให้เกิด exit code 2: command line error)
- **มาตรฐาน**: ใช้ `--progress-bar off` ร่วมกับ `-q --prefer-binary` เสมอ
- **ตัวอย่าง**:
  ```bash
  pip install -q --progress-bar off --prefer-binary -r requirements.txt
  ```

### 2. ⚡ BitsAndBytes CUDA Auto-Detection Standard
- **กฎ**: ห้ามบังคับตั้งค่า `BNB_CUDA_VERSION=121` ล็อกตายตัวบน PyTorch / CUDA 12.8+
- **มาตรฐาน**: ปล่อยให้ `bitsandbytes` ทำการ Auto-detect Native CUDA Libraries (`libbitsandbytes_cuda128.so`) อัตโนมัติ โดยสั่ง `os.environ.pop("BNB_CUDA_VERSION", None)`

### 3. 🔗 Dependency Compatibility Matrix Standard (MLOps / AISDLC)
- **ชุดแพ็กเกจที่เข้ากัน 100% (Locked Compatible Stack)**:
  - `transformers == 4.44.2`
  - `peft == 0.12.0`
  - `trl == 0.11.0`
  - `accelerate == 0.33.0`
  - `bitsandbytes == 0.43.3`
  - `datasets == 2.18.0`
  - `huggingface_hub == 0.25.1`
- **กฎ**: หากเลือกใช้ `trl 0.12.0+` จะต้องอัปเกรดทั้งชุดขึ้นไป `transformers>=4.46.0` เท่านั้น ห้ามผสมข้ามเวอร์ชัน

### 4. 🧹 Pure ASCII Logging & Noise Suppression (Unicode Surrogate Guard)
- **กฎ**: ข้อความใน `logger` และ `sys.stdout` จะต้องเป็น Pure ASCII (ใช้ ASCII tags เช่น `[OK]`, `[ERROR]`, `[WARNING]`, `[INFO]`, `[START]`, `[MODEL]`, `[CUDA]`, `[AUDIT]` ห้ามใช้ Emoji ใน subprocess ล็อก)
- **การตัด Noise**: ตั้งค่า Environment Variables ต่อไปนี้ก่อนเริ่มทำงานเสมอ:
  ```python
  os.environ["PYTHONIOENCODING"] = "utf-8:surrogateescape"
  os.environ["PYTHONUTF8"] = "1"
  os.environ["HF_HUB_DISABLE_PROGRESS_BARS"] = "1"
  os.environ["TRANSFORMERS_VERBOSITY"] = "error"
  os.environ["TQDM_DISABLE"] = "1"
  ```

### 5. ⚡ Fail-Fast Package Verification Guard
- **กฎ**: ก่อนปล่อยให้สคริปต์ดาวน์โหลด Weights ขนาดใหญ่ ต้องรันเซลล์ยืนยันการ Import แพ็กเกจหลักก่อนเสมอ:
  ```python
  import transformers, peft, trl, bitsandbytes, datasets, accelerate
  ```

### 6. 🛡️ Native PyTorch Environment & CUDA Compatibility Guard (Kaggle/Cloud Rule)
- **กฎ**: ห้ามรัน `pip install torch==...` เพื่อ Reinstall PyTorch ทับใน Kaggle / Cloud Environment โดยเด็ดขาด เพราะจะทำให้ cuDNN (`libcudnn.so.8`) และ C++ extensions พังเสียหาย
- **มาตรฐาน**: ใช้ PyTorch ที่ pre-installed มากับ Kaggle Base Container เสมอ และในกรณีที่พบ GPU สถาปัตยกรรมที่ไม่รองรับ CUDA binary (เช่น `sm_60` / Tesla P100) สคริปต์ `cloud_train_orchestrator.py` ต้องทำการ Graceful Fallback ไปยัง CPU Execution Mode อัตโนมัติ เพื่อรับประกัน Exit Code 0 100%

---

## 📊 Kanban Board Standard
- ทุกครั้งที่มีการแก้ไขและป้องกันปัญหา ให้บันทึกสาเหตุ (Root Cause) และมาตรการป้องกัน (Prevention) ลงใน `PROJECT_TASKS.md` หมวด **DONE** และ **DOING** เสมอ

