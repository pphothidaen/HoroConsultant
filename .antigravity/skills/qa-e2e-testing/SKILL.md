---
name: qa-e2e-testing
description: Pytest suite, UI button contracts, and Playwright visual E2E regression tests.
---

# 🧪 QA & E2E Testing Skill Guide

This skill specifies testing standards, regression execution commands, and log filtering protocols for the QA Tester and Developer agents.

---

## 🛠️ Testing Command Suite

### 1. Pytest Unit & Integration Regression Suite
Run full test suite with verbose output:
```bash
python3 -m pytest -v --ignore=project/kaggle_kernel
```
*Goal*: Ensure 100% pass rate across all 123 test cases (BaZi, Swiss Ephemeris, 5-branch Metaphysics, RAG, MCP, API Router).

### 2. UI Button & Endpoint Contract Regression Suite
Run full 25-button/endpoint contract check across `index.html` (Main Dashboard), `admin.html` (Admin Panel), `hitl.html` (HITL Review Studio), and `OpenAPI Documentation` (`/docs`, `/redoc`, `/openapi.json`):
```bash
python3 scripts/run_button_regression.py
```
*Report Output*: `project/tests/button_regression_report.json`

### 3. Live Production E2E Playwright Automation Suite (Hugging Face Spaces)
Run browser automation on the live hosted Space (`https://pphothidaen-horoconsultant-core-backend.static.hf.space/index.html`) across 10 live test cases:
```bash
python3 scripts/run_live_e2e_hf_space.py
```
*Test Coverage*:
1. Live Main Dashboard Load (HTTP 200)
2. Dynamic AI Query 1 — Children & Offspring (`"ลูกเป็นอย่างไร"`)
3. Dynamic AI Query 2 — Business Investment 2026 (`"ปี 2026 ควรเปิดร้านอาหารดีไหม"`)
4. Dynamic AI Query 3 — Love & Relationships (`"เรื่องความรักปีนี้จะเจอคู่ไหม"`)
5. Dynamic AI Query 4 — Wealth & Financial Fortune (`"เรื่องการเงินและโชคลาภปีนี้มีเกณฑ์เป็นอย่างไร"`)
6. Dynamic AI Query 5 — Health & Wellness (`"สุขภาพเรื่องกระดูกและสายตามีแนวโน้มเป็นอย่างไร"`)
7. Location Resolution (Subdistrict Name -> Longitude)
8. Interactive Metaphysics Discipline Buttons (6 disciplines)
9. Live Admin Panel Load (`admin.html`)
10. Live HITL Review Studio Load (`hitl.html`)

*Report Output*: `project/tests/live_e2e_report.json`  
*Screenshots Directory*: `project/tests/screenshots/live_e2e/`

---

## 🛡️ QA Rules & Production Verification Mandates

1. **Zero Superficial Symptom Patching**:
   - Never resolve failing tests by commenting out assertions, deleting tests, returning dummy fallbacks, or swallowing exceptions in silent try/except blocks.
   - Trace upstream root causes in source modules.

2. **Strict Real AI / LLM Inference Verification (Anti-Template Mandate)**:
   - QA MUST verify that all astrological interpretation responses are generated by genuine AI Agent (LLM) inference rather than static string templates or placeholder echo generators.
   - Assert domain-specific keywords and classical BaZi constructs (Ten Gods 十神, 5 Elements 五行, Palaces 柱) across multiple distinct query domains.

3. **Production Cloud Endpoint & Network Error Audit**:
   - QA MUST audit browser console logs and network traffic during live Playwright testing to ensure zero unhandled CORS exceptions or HTTP 404/502 backend failures.

4. **Log Extraction Protocol**:
   - Extract only relevant error snippets (file, line number, exception traceback) when reporting bugs to the Developer or Orchestrator.
   - Keep log output concise to preserve token context window.

5. **Post-Deployment Mandatory Verification**:
   - After any push to `main` or production release, always re-run `python3 scripts/run_live_e2e_hf_space.py` and `python3 scripts/run_button_regression.py` to confirm live production system functionality.
