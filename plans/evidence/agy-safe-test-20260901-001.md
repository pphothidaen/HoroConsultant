# AGY Safe Admission Test Evidence — AGY-SAFE-TEST-20260901-001

- Status: DONE (local policy-test scope only)
- Owner: qa_tester
- Recorded at: 2026-09-01T03:46:21Z
- User authorization: approval for `agy1`–`agy4`, constrained by this ticket to non-provider local tests.

## Command and result

```text
python3 -m pytest -q tests/test_multiagent_receipt_schema.py tests/test_agy_bucket_admission_integration.py tests/test_agy_cli_docs_inspector.py tests/test_four_pool_two_root_capacity.py tests/test_multiagent_prompt_command.py::test_governed_account_aliases_includes_agy3_and_codex3
exit status: 0
result: 36 passed in 0.35s
```

The command ran only Python tests. It did not execute an `agy` command, invoke a provider process, use a network request, or inspect credentials.

## Verified local controls

- `tests/test_multiagent_receipt_schema.py::test_generated_codex_receipt_matches_v3_schema_and_agy_generates_no_receipt` replaces the AGY transport with a forbidden callable, requires `PLATFORM_NATIVE_PRESPAWN_RECEIPT_REQUIRED`, and proves the callable was not reached.
- `tests/test_agy_bucket_admission_integration.py::test_fake_runner_proves_no_provider_command_or_dispatcher_invocation_on_invalid_artifact` rejects an unsafe artifact while both injected provider and dispatcher call counters remain empty.
- `tests/test_agy_cli_docs_inspector.py` passed its 11 local sanitization and allowed-command inspection cases; this is CLI-document parsing coverage, not an AGY CLI invocation.
- `tests/test_four_pool_two_root_capacity.py` passed its five lease/capacity cases for the defined local policy pools and confirms configured local caps are not provider-native capacity proof.
- `tests/test_multiagent_prompt_command.py::test_governed_account_aliases_includes_agy3_and_codex3` passed and proves command-level registration for `agy3` with provider mapping `agy`.

## Alias coverage and limitations

| Alias | Local test evidence | Provider execution evidence |
| --- | --- | --- |
| `agy1` | Admission integration and local capacity-policy coverage | None |
| `agy2` | Local capacity-policy coverage | None |
| `agy3` | Command-level governed-alias registration and provider mapping | None |
| `agy4` | No configuration or test registration was found in this bounded suite | None |

The current capacity test defines only `agy1` and `agy2`; its configured caps are governance values, not runtime proof. The current command test covers `agy3`, not `agy4`. Therefore this receipt does not establish four-alias registration, quota, capacity, account health, a native pre-spawn receipt path, or any AGY/provider execution.

## Conclusion

Local fail-closed policy behavior is verified. `PLATFORM_NATIVE_PRESPAWN_RECEIPT_REQUIRED` remains the expected boundary before AGY transport. Any provider invocation still requires a current native platform pre-spawn receipt, valid capacity lease, fresh quota/telemetry proof, and a separately authorized bounded dispatch.
