{
  "schema_version": 1,
  "ticket": "TICKET-RELEASE-COMPLETE-20260826-40B1-SECRET-SYNC-ENV",
  "status": "DONE_FROZEN",
  "ownership": ["scripts/sync_doppler_secrets.py", ".env.example"],
  "contract": {
    "default": "validates the example only; no implicit local environment file or network mutation",
    "apply": "requires both --apply and explicit --env-file",
    "retired_keys": "Azure and Fly prefixes excluded without names or values in logs",
    "preserved": "HF Docker backend and Vercel settings remain",
    "redaction": "secret values are never printed"
  },
  "external_actions": "none"
}
